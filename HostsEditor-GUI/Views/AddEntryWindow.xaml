<Window x:Class="HostsEditor_GUI.Views.AddEntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加条目" Width="360" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize"
        Background="{DynamicResource Brush.Window.Background}"
        Foreground="{DynamicResource Brush.Window.Foreground}">
    <StackPanel Margin="16">
        <TextBlock Text="IP 地址" />
        <TextBox x:Name="IpBox" Style="{DynamicResource TextBoxStyle}" Margin="0,4,0,12" />
        <TextBlock Text="主机名" />
        <TextBox x:Name="HostBox" Style="{DynamicResource TextBoxStyle}" Margin="0,4,0,12" />
        <TextBlock Text="分组（留空为默认分组）" />
        <ComboBox x:Name="GroupBox" IsEditable="True" Style="{DynamicResource ComboBoxStyle}"
                  ItemContainerStyle="{DynamicResource ComboBoxItemStyle}" Margin="0,4,0,16" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Style="{DynamicResource AccentButton}" Click="OnOk" IsDefault="True" />
            <Button Content="取消" Style="{DynamicResource DefaultButton}" Click="OnCancel" IsCancel="True" Margin="8,0,0,0" />
        </StackPanel>
    </StackPanel>
</Window>
