<Window x:Class="HostsEditor_GUI.Views.AddEntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加条目" Width="360" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize">
    <StackPanel Margin="16">
        <TextBlock Text="IP 地址" />
        <TextBox x:Name="IpBox" Margin="0,4,0,12" />
        <TextBlock Text="主机名" />
        <TextBox x:Name="HostBox" Margin="0,4,0,12" />
        <TextBlock Text="分组（留空为默认分组）" />
        <ComboBox x:Name="GroupBox" IsEditable="True" Margin="0,4,0,16" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Click="OnOk" IsDefault="True" />
            <Button Content="取消" Click="OnCancel" IsCancel="True" Margin="8,0,0,0" />
        </StackPanel>
    </StackPanel>
</Window>
